
install.packages("dplyr")
library(dplyr)

arrange(, , )

head(arrange(mtcars, wt))


mpg(rank)
rank(mpg)

cy1 <- c(33123,421,4,1,2,51,61,6,57,467,3,745,56,3,23521,4,12)

distinct(mtcars, cy1)

summarise(mtcars, mean(cy1), min(cy1) , max(cy1) )

summarise(gr_cy1, n())
